// Swap two numbers using 3rd variable

#include <stdio.h>
#include<math.h>
int main()
{
    int num = 8;
    int power1 = pow(num, 2);
    printf("power of %d = %d", num, power1); // Output: power 8 = 64
}